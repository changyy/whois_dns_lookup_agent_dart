name: whois_dns_lookup_agent
description: Pure-Dart multi-server DNS lookup + Whois client. CLI emits JSON suitable for shell pipelines.
version: 1.20260524.114512
repository: https://github.com/changyy/whois_dns_lookup_agent_dart
homepage: https://github.com/changyy/whois_dns_lookup_agent_dart
issue_tracker: https://github.com/changyy/whois_dns_lookup_agent_dart/issues

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0

executables:
  whois_dns_lookup_agent: whois_dns_lookup_agent

topics:
  - dns
  - whois
  - networking
  - cli

platforms:
  android:
  ios:
  linux:
  macos:
  windows:
